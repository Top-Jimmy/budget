Documentation
    https://firebase.google.com/docs/functions/local-emulator

Install Firebase CLI
$ npm install -g firebase-tools

Run local emulator
$ firebase emulators:start
Run only local functions
$ firebase emulators:start --only functions
